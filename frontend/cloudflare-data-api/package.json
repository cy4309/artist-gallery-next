{
  "name": "cyc-data-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute cyc-data --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute cyc-data --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
